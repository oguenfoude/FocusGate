<ui:FluentWindow x:Class="FocusGate.Desktop.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="FocusGate" Width="1100" Height="700"
        MinWidth="800" MinHeight="500"
        WindowStartupLocation="CenterScreen"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="FocusGate" ShowMaximize="False"
                     CloseWindowByDoubleClickOnIcon="False">
            <ui:TitleBar.Icon>
                <ui:SymbolIcon Symbol="Shield24" />
            </ui:TitleBar.Icon>
        </ui:TitleBar>

        <Border Grid.Row="1" Background="{DynamicResource ApplicationBackgroundBrush}"
                BorderBrush="{DynamicResource ControlStrongStrokeColorDefaultBrush}"
                BorderThickness="0,0,0,1" Padding="24,10">
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock x:Name="MachineIdText" FontSize="12" FontWeight="SemiBold"
                               Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                               VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <ui:Button x:Name="RestartButton" Click="RestartButton_Click"
                               Appearance="Transparent" Icon="{ui:SymbolIcon ArrowSync24}"
                               ToolTip="Restart Hardware service" Padding="8,6"
                               Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
                    <ui:Button x:Name="StopButton" Click="StopButton_Click"
                               Appearance="Transparent" Icon="{ui:SymbolIcon DismissCircle24}"
                               ToolTip="Stop FocusGate system" Padding="8,6"
                               Foreground="#ef4444"/>
                    <ui:Button x:Name="MuteButton" Click="MuteButton_Click"
                               Appearance="Transparent" Icon="{ui:SymbolIcon SpeakerMute24}"
                               ToolTip="Toggle SMS notifications" Padding="8,6"
                               Foreground="#10b981"/>
                </StackPanel>
            </Grid>
        </Border>

        <Frame x:Name="ContentFrame" Grid.Row="2" NavigationUIVisibility="Hidden"/>
    </Grid>
</ui:FluentWindow>

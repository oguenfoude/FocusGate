<ui:FluentWindow x:Class="FocusGate.Desktop.Views.SmsDetailDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Message" Width="520" Height="420"
        WindowStartupLocation="CenterOwner"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="Message" ShowMaximize="False" ShowMinimize="False"/>

        <ui:CardControl Grid.Row="1" Margin="24,16,24,0" Padding="20"
                         Icon="{ui:SymbolIcon Chat24}">
            <StackPanel>
                <TextBlock x:Name="SenderText" FontSize="18" FontWeight="ExtraBold"
                           Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
                <TextBlock x:Name="DateText" FontSize="12" Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                           Margin="0,4,0,0"/>
            </StackPanel>
        </ui:CardControl>

        <Border Grid.Row="2" Margin="24,12,24,0" Background="{DynamicResource ApplicationBackgroundBrush}"
                CornerRadius="12" Padding="20"
                BorderBrush="{DynamicResource ControlStrongStrokeColorDefaultBrush}" BorderThickness="1">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="ContentText" FontSize="15"
                           Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                           TextWrapping="Wrap" LineHeight="24"/>
            </ScrollViewer>
        </Border>

        <Border Grid.Row="3" Margin="0,12,0,0" Padding="24,16"
                BorderBrush="{DynamicResource ControlStrongStrokeColorDefaultBrush}"
                BorderThickness="0,1,0,0" Background="Transparent">
            <ui:Button Click="CloseButton_Click" Appearance="Primary"
                       Content="Close" HorizontalAlignment="Right" Padding="24,8"/>
        </Border>
    </Grid>
</ui:FluentWindow>

<Page x:Class="FocusGate.Desktop.Views.ModemDetailPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      Background="Transparent">
    <Grid Margin="32,28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ui:Button Grid.Row="0" HorizontalAlignment="Left" Click="BackButton_Click"
                   Appearance="Transparent" Icon="{ui:SymbolIcon ArrowLeft24}"
                   Content="Back to Modems" Margin="0,0,0,24"/>

        <ui:CardControl Grid.Row="1" Padding="32,28" Margin="0,0,0,28"
                         Icon="{ui:SymbolIcon Phone24}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="PHONE NUMBER" FontSize="12" FontWeight="Bold"
                               Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,0,6"/>
                    <TextBlock x:Name="PhoneNumberText" FontSize="32" FontWeight="ExtraBold"
                               Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
                </StackPanel>
                <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                    <TextBlock Text="BALANCE" FontSize="12" FontWeight="Bold"
                               Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                               HorizontalAlignment="Right" Margin="0,0,0,6"/>
                    <TextBlock x:Name="BalanceText" FontSize="32" FontWeight="ExtraBold"
                               Foreground="{DynamicResource SystemAccentColor}" HorizontalAlignment="Right"/>
                </StackPanel>
            </Grid>
        </ui:CardControl>

        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Messages" FontSize="22" FontWeight="Bold"
                           Foreground="{DynamicResource TextFillColorPrimaryBrush}" VerticalAlignment="Center"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Ellipse x:Name="StatusIndicator" Width="10" Height="10" Fill="{DynamicResource ControlStrongStrokeColorDefaultBrush}"
                             Margin="0,0,8,0" VerticalAlignment="Center"/>
                    <TextBlock x:Name="StatusText" FontSize="14" FontWeight="SemiBold"
                               Foreground="{DynamicResource TextFillColorSecondaryBrush}" VerticalAlignment="Center"/>
                    <TextBlock Text=" | " Foreground="{DynamicResource TextFillColorTertiaryBrush}" Margin="8,0"/>
                    <TextBlock x:Name="ImeiText" FontSize="14" FontWeight="SemiBold"
                               Foreground="{DynamicResource TextFillColorSecondaryBrush}" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>

            <ListBox Grid.Row="1" x:Name="SmsList" Background="Transparent" BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     MouseDoubleClick="SmsList_MouseDoubleClick">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Margin" Value="0,0,0,12"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border x:Name="Bd" Background="{DynamicResource ApplicationBackgroundBrush}"
                                            BorderBrush="{DynamicResource ControlStrongStrokeColorDefaultBrush}"
                                            BorderThickness="1" CornerRadius="12" Padding="20">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Grid.Column="0">
                                                <TextBlock Text="{Binding SenderNumber}" FontWeight="Bold" FontSize="16"
                                                           Foreground="{DynamicResource TextFillColorPrimaryBrush}" Margin="0,0,0,6"/>
                                                <TextBlock Text="{Binding Content}" Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                                           FontSize="14" TextWrapping="Wrap" MaxHeight="40"
                                                           TextTrimming="CharacterEllipsis"/>
                                            </StackPanel>
                                            <TextBlock Grid.Column="1" Text="{Binding ReceivedAt}"
                                                       Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                                                       FontSize="12" Margin="16,0,0,0" VerticalAlignment="Top"/>
                                        </Grid>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Bd" Property="Background"
                                                    Value="{DynamicResource ControlFillColorDefaultBrush}"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Bd" Property="Background"
                                                    Value="{DynamicResource ControlFillColorDefaultBrush}"/>
                                            <Setter TargetName="Bd" Property="BorderBrush"
                                                    Value="{DynamicResource SystemAccentColor}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>

            <ui:InfoBar x:Name="NoSmsText" Severity="Informational" IsOpen="True"
                        Title="No messages yet"
                        Message="SMS will appear here when received."
                        Visibility="Collapsed"
                        Margin="0,0,0,0"/>
        </Grid>
    </Grid>
</Page>
